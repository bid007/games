## This repo contains my pet projects done to learn C++.

### Tictactoe is a basic tictactoe game written using C++ and raylib. I am using Bazel for building it. It works fine on M1 silicon. You can try this running the following bazel command.

```
bazel run //tictac:main
```
---
